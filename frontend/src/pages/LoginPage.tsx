const LoginPage = () => {
  return (
    <div className="rounded-2xl border border-slate-800 bg-slate-900/60 p-6 text-white shadow-lg">
      <h1 className="text-xl font-semibold">Login (Coming Soon)</h1>
      <p className="mt-2 text-sm text-slate-300">
        Authentication will be added in a future update. For now, use the mock login button in the header to simulate a signed-in state.
      </p>
    </div>
  );
};

export default LoginPage;

